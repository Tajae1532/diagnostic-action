{
  "name": "diagnostic-action",
  "version": "1.0.0",
  "description": "Pipeline Failure Diagnostic — analyzes CI build failures and prints a plain-English diagnosis inline in your pipeline logs.",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build index.js -o dist --source-map --license licenses.txt",
    "prepare": "npm run build"
  },
  "keywords": [
    "github-actions",
    "ci",
    "devops",
    "pipeline",
    "diagnostic",
    "build-failure"
  ],
  "author": "tajae1532",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}